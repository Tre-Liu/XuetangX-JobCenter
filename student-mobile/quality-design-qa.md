# 素质图谱移动端验收

final result: passed

## 依据和范围

源图：用户提供的 codex-clipboard-145945ea-6016-4ac5-a6db-20498577d38e.png、codex-clipboard-3fdcf75e-e9de-4286-b090-010ed1d672e5.png、codex-clipboard-9eb9a9a1-9e22-4996-86b7-b7c78923ffea.png、codex-clipboard-71b368e4-b10d-4ff1-b38b-ceadfe49c872.png，均位于 /var/folders/zq/0shk2lcn5lz9ncw39dykp0vm0000gn/T/，原尺寸 3560×2132。
这是将桌面设计语言适配手机，不是桌面像素级复制：框架从五列改为纵向分组，树改为纵向分枝，右侧详情改为底部面板。

实现：http://localhost:5173/；课程 → 更多 → 图谱 → 素质图谱。
截图：qa-quality/framework.png、qa-quality/tree.png、qa-quality/detail.png、qa-quality/android-ring.png。截图为 1400×1200 浏览器视口，iPhone 屏幕经 DOM 测量为 393×852、Pixel 10 为 427×952，均为 1:1 CSS 尺寸。比较仅关注设备屏幕内应用区域，不比较桌面浏览器、设备边框和页面外留白。源图与实现截图在同一组视觉检查输入中复核。

## 已修复的问题与复查

- P2：初版圆环长名称换行过碎。增大维度节点，修正字号和行距；课程根节点下移，避免与邻近叶子文字重叠。Android 圆环截图复核完整名称可读。
- P1：关闭按钮使用负定位，落在详情滚动区域裁切范围；聚焦引起外层手机屏幕偏移。改成内容区内按钮，限制外层屏幕的非预期滚动，保留内部 MobileScroll；复测详情打开与关闭、screen.scrollTop 为 0。
- P1：框架 MobileScroll 默认绝对定位导致内容覆盖顶部导航。改为参与 flex 布局的相对定位。framework.png 复查导航、视图切换和内容互不覆盖。

## 五项视觉复核

- 字体：沿用中文系统字体。标题 17px，切换 13px，框架节点 13px；圆环与树文字随缩放，概览可辨识，提供放大和框架阅读入口。
- 间距：手机安全区与设备组件未改。框架最后一组可滚动到达，工具条位于底部安全区上方。
- 色彩：沿用红、金、橙、棕的类别色，暖色渐变节点、浅粉底与淡黄背景。类别配色在三视图中保持一致。
- 图像：图谱为可交互的数据可视化，不使用整张参考截图替代节点。图标使用现有 Radix 图标库；空态改为轻量文档图标，未仿造桌面纸箱插画。
- 内容：5 个维度、16 个叶子节点与参考一致；保留参考课程名；节点关联内容未虚构。

## 交互证据

已验证三视图切换、圆环和树的节点详情、维度到叶子的连续浏览、详情关闭、缩放与适应屏幕。
拖动：树画布 transform 从 translate(6.32446px,-33.2266px) 到 translate(61.3245px,5.77339px)，缩放维持 0.667283，未打开详情（dialog 数为 0）。
框架滚动：clientHeight=616、scrollHeight=1066、scrollTop=450.5，底部合作素养可点开。
设备：iPhone 和 Pixel 10 已视觉检查；浏览器捕获到的 error 日志为空。
双指缩放逻辑已实现，但当前浏览器工具没有直接模拟双指操作，未声明物理真机触摸验收。

## 余项

无待修复 P0/P1/P2。当前是本地参考数据原型，关联内容接口尚未接入。
