# 课程 CMS

入口为本目录 index.html；源码位于 ../src/cms/。

课程页面顶部“课程 CMS”进入管理后台，点击“配置课程”，设置专业、确认匹配的产业链、开启产教模型，然后“保存并返回课程”。不启用产教模型时只有 AI 生成框架，开启后显示岗位任务驱动/项目主题驱动菜单。

本地演示配置保存在浏览器。直接打开文件时使用页面上的往返入口传递配置，避免文件页面之间的存储隔离。独立页面随 npm run build 自动更新，勿手工编辑生成后的 index.html。
