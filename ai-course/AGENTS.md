# Prototype Instructions

岗位生成的知识点匹配步骤在无可用知识点（含演示开关为“无”或图谱无二级及以下知识点）时默认选中“暂不匹配”，可直接下一步。同步源码、index.html 与 offline.html。

岗位与任务步骤已有默认岗位时，等待助手提示展示完成、典型任务区域挂载后，平滑滚动到“选择一个典型工作任务”；手动切换岗位保持同样引导。同步 index.html、offline.html。

知识图谱主画布左下角保留隐蔽的知识点有/无演示开关：鼠标移入或键盘聚焦后显现入口，点击选择状态；状态在浏览器记忆，并同步项目任务的知识点关联列表。“无”展示空图谱和空关联列表；切换不删除图谱、教师编辑或已有任务关联，切回“有”恢复。源码与根目录 index.html、offline.html 同步构建。

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## AI 课程的岗位项目设计

项目页「AI 生成框架」为下拉按钮，提供「岗位任务驱动 / 项目主题驱动」两个菜单项，分别直接打开岗位工作任务向导和原有生成弹窗。移除弹窗内的模式开关；支持点击外部、Escape 关闭与键盘选择。两种模式继续覆盖当前项目。同步构建根目录 index.html 与 offline.html。

岗位项目生成时，AI 课已关联专业则继承课程专业并锁定，不能修改或清空；未关联则必须通过官方目录选择一个专业，支持教育层次、专业大类/学科门类、专业类级联及名称/代码搜索。课程绑定独立于生成草稿，历史草稿不得覆盖已有绑定。人培方案选填；移除“启用该专业的产教模型”复选框及启用前置条件。只按已选专业、人培筛选参考岗位，两者都选时保留专业一致性校验；无合适岗位时允许手动补充。按用户附件的学习型工作任务方法进行教学化转化，保留岗位任务与原子能力出处，区分资料参考、AI/规则草稿与教师修订。课程页面须同步构建可通过 Finder 打开的根目录 index.html。

岗位项目生成弹窗不展示左侧“转化方法依据”说明块，也不展示底部“本地原型 · 关联信息与生成草稿仅保存在此浏览器”提示。

新增「项目式学习」弹窗以用户提供的雨课堂截图为准：标题为“新增”，包含三种可选 ICON、必填中文标题、选填英文标题、必填中文描述、选填英文描述，底部“取消 / 确定”；中文必填项未填完整时禁用“确定”。保存图标及中英文字段，编辑时回填；同步根目录 index.html 与 offline.html。

“添加任务内容”使用用户截图中的按钮下方白色浮层：计分区为知识点、学习单元两张卡片，不计分区为知识库、云盘、本地文件三个并排按钮。保留截图文案与蓝/绿配色，支持点击外部和 Escape 关闭；内容来源与计分口径随所选入口保存，同步 index.html 与 offline.html。

项目任务的“知识点”入口打开“关联知识点”列表，和知识图谱共用节点数据及 ID，支持名称搜索、2/3/4 级筛选、多选及当前查询结果全选；关联只包含所选节点和它的直接学习单元，不递归包含子节点。“学习单元”入口打开“关联课程资源”，包含名称搜索、图文/视频类型筛选及多选；列表以用户第二张截图的图文资源为初始记录，保留来源证据，不捏造文件正文。已关联项禁止重复添加，无选择时确定禁用；图谱改名后任务名称同步。

点击项目页“预览”切换主内容区为横向阶段看板，保留顶部与左侧导航；不同颜色阶段列用箭头连接，列内任务卡展示当前关联内容、资源类型及计分标签，空内容显示“暂无任务内容”。预览不提供编辑操作，溢出横向滚动；“退出预览”恢复编辑态与折叠状态。源码与 index.html、offline.html 同步构建。

项目学习说明默认展示简易岗位能力图谱：学习型工作任务关联典型工作任务与岗位，典型任务可展开本学习任务关联的能力项，岗位可向上展开产业环节与产业链。沿用已有岗位、任务、能力 ID 与来源；产业归属缺失时展示未关联，历史推断保留待复核标记。源码、index.html、offline.html 同步构建。

项目预览采用“项目步骤 / 岗位关联图谱”两个互斥可切换视角，默认项目步骤。岗位关联图谱复用成果页的 JobCompetencyMap 图谱形态和节点展开交互，独占主内容区，默认展开产业归属；不使用横向五卡片，不叠放在步骤看板上方。保留当前项目、典型工作任务、岗位、产业环节、产业链及关联能力项，产业关系保留原始来源与复核状态，缺失时显示未关联，不按课程名称猜测。兼容已有浏览器草稿，源码与 index.html、offline.html 同步构建。

项目式学习侧栏每个项目右侧提供省略号菜单，包含“编辑 / 删除”。编辑打开“修改”弹窗并回填图标、中英文标题和描述；删除弹出含项目名称的不可恢复二次确认，“取消 / 确定”。操作绑定所点项目；删掉当前项目后切到剩余项目，最后一个项目删除后保留空状态，刷新不得恢复示例。源码与 index.html、offline.html 同步构建。

课程关联图谱以用户提供的专业全景图谱（public/opendesign/industry-education-graph-prototype.html）为视觉依据：分区、半透明节点、正交关系线与选中高亮。只展示当前课程及其左右真实关联链路，按已保存数据隐藏缺失维度，不展示培养目标、毕业要求、课程目标的占位或草稿。产业、专业、课程图谱的开关删除；保留项目步骤与图谱两个独立视角。专业、产业链、产业环节、岗位、项目任务、能力项、知识点按实际关联展示，知识点仅取项目明确关联且可用的节点，禁止导入参考图里的无关专业课程及全量知识库。覆盖前述复用 JobCompetencyMap 的预览要求，原生成成果说明不受影响。

课程全景图谱点击节点时点亮完整上下游链路（含课程、专业和产业来源），强化关联节点与连线，淡化其他分支；不能从课程或共享能力项回头遍历并误亮无关项目。再次点击所选节点、点击画布空白或按 Escape 恢复全图。移除底部节点详情、关联依据、关系按钮区域；专业节点不展示学校及人培说明，来源数据仍保留。同步 index.html、offline.html。

课程全景图谱的项目任务右侧展示其已关联的课程知识点；岗位能力项位于左侧岗位工作任务下方，并连接所属岗位任务。依据同一项目任务已保存的能力 ID 与知识点 ID 建立能力—知识点连线，保留经由项目任务的关联依据；不能将岗位能力直接改名为知识点或按全项目笛卡尔积连线。共享能力不得导致无关项目或任务的知识点误高亮；知识点无关联或不可用时不虚构节点。同步 index.html、offline.html。

项目任务正文统一为具有教学属性的“任务说明”：整块自由文本，点击原位编辑、自动保存，不在任务编辑弹窗中分字段修改。说明结合学习型工作任务方法，写清任务情境、学习目标、实施步骤、成果与评价、学习准备和支持；兼容历史活动草稿，保留教师修改与主动清空的文本。预览使用同一份说明且只读，同步 index.html 与 offline.html。

项目任务操作以用户 2026-09-07 16:36 录屏为准：“创建新任务”立即在当前阶段末尾新增独立空白任务卡，拥有独立标题、内容、折叠和删除操作；新增后定位并聚焦新卡片。“添加任务内容”只向所点任务关联知识点或学习单元，可在同一任务内连续添加多种内容，不额外生成任务、不写入其他任务。保留任务说明编辑，源码与 index.html、offline.html 同步构建。

岗位项目生成的“岗位与任务”步骤中，不展示手动能力草稿标题下的本地规则说明；知识、技能、素养均支持教师逐项添加与删除，新增默认勾选且可编辑，删除同步移除选择及后续项目关联。教师新增项保留独立来源，不能标作人培原文；同步 index.html 与 offline.html。

岗位项目生成的“教学化转化”步骤不展示“学习任务类型”“学习者与学情”表单项，以及底部“能力目标来自已选……”说明；其余表单按网格自然补位。源码与 index.html、offline.html 同步构建。

项目学习说明移除顶部“学习型工作任务 / 岗位名称”标签行；核心问题、学情与先备条件、项目成果合并为整块自由文本“项目说明”，复用任务说明的原位编辑与自动保存交互。保留历史文字、教师修订和主动清空；预览只读，岗位能力图谱与来源保留。同步 index.html 与 offline.html。

岗位项目生成的“课程关联”步骤不展示“载入示例关联”按钮。源码与 index.html、offline.html 同步构建。

岗位项目生成的“审阅项目”步骤不展示“教师审阅要点”及勾选确认区，直接通过“确认并创建项目”提交。每次前进、返回和创建项目均展示 loading 与对应提示，期间禁用重复提交，关闭弹窗或切换模式时取消待执行操作；校验失败立即提示，不进入 loading。同步 index.html 与 offline.html。

岗位项目生成的第二步补充分支改为“根据课程与任务生成”：课程名称默认带入当前课程，项目任务名称由教师填写，按钮为“生成任务内容”。依据两个名称生成可编辑的任务说明与知识、技能、素养草稿；名称变更后清除旧草稿，保留本地规则来源标记，不将课程名冒充岗位名。同步 index.html 与 offline.html。

知识点详情在“思政设计”后新增“岗位关系”页签，以列表展示岗位、典型工作任务、岗位能力项及来源项目任务。依据同一项目任务明确关联的知识点 ID 和能力 ID 匹配，合并重复关系并保留来源；逐条删除后持久化排除，保留原项目教学内容，刷新不恢复已删除关系。源码与 index.html、offline.html 同步构建。

知识点“岗位关系”内置 5 个知识点、10 条明确标注“模拟”的教学演示关系，历史图谱草稿按稳定知识点 ID 补充演示数据，保留教师编辑与删除记录。图谱顶部提供“岗位关系示例”直达列表；模拟项目数据独立于教师项目，不能冒充真实项目记录。同步 index.html 与 offline.html。

岗位生成第二步按用户 2026-09-07 最新标注保留“选择产业岗位 / 自定义岗位”两个页签，覆盖先前“根据课程与任务生成”页签要求。先选择岗位，再单选该岗位下一个典型工作任务，最后勾选能力项；选择岗位不得自动选择首个典任，切换典任清除旧能力选择。“自定义岗位”中提供“添加岗位”，允许自主填写岗位名称、多条典任与知识/技能/素养能力；辅助生成是可选操作，保留草稿来源。自定义岗位在当前课程本地保存，支持再次选择和修订，生成项目只关联所选典任及其能力。同步 index.html 与 offline.html。

项目内“AI 生成框架”的普通模式和岗位任务模式均覆盖当前项目数据，不新增项目、不追加旧阶段；保留项目 ID 和侧栏位置，其他项目不受影响。岗位生成覆盖标题、说明、教学设计、岗位关联、阶段任务及内容；普通模式替换阶段任务并清理旧教学设计。提交与 loading 文案使用“覆盖当前项目”。此要求覆盖前述追加和创建项目规则，同步 index.html 与 offline.html。

2026-09-08：整体 AI 生成采用对话流引导选择或填写；岗位流程仍为课程关联→岗位与任务→教学化转化→审阅项目，保留回答记录和返回修改。独立课程 CMS 子目录 cms/ 管理课程专业和产教模型：专业→匹配并确认产业链→开启模型。生成专业以 CMS 配置为准；未配置时去 CMS 设置。仅有效开启产教模型时显示“岗位任务驱动 / 项目主题驱动”下拉菜单，否则直接进入“AI 生成框架”。更换专业清空旧产业链并关闭模型。此要求覆盖前述始终显示菜单、生成内选择专业和不设模型前置条件的旧规则。保持 file 入口配置往返与 HTTP 联动，同步构建 index.html、offline.html、cms/index.html。

2026-09-08 后续更正：普通“AI 生成框架 / 项目主题驱动”恢复原确认弹窗，展示当前项目、自动生成阶段与任务、覆盖提示，底部“取消 / 开始生成”；不使用对话流、不增加目标成果填写。仅岗位任务驱动保留对话流。CMS 开关联动保留，普通生成仍只替换当前项目阶段任务、清除旧岗位教学设计，保留项目名称、说明、ID 及其他项目。同步所有静态入口。

课程 CMS 视觉必须沿用 major-construction-platform/industry-research-admin.html：216px 深蓝侧栏（#2f4053）、平台管理展开菜单、白色58px顶栏、浅灰背景、整块白色管理面板与横向管理页签。禁止白侧栏、雨课堂大品牌区与英文大标题布局；同步 cms/index.html。

CMS“基础信息 / 产教模型”必须是真正的互斥 Tab 面板，不使用锚点跳转、不把两块内容上下通栏排列。默认基础信息；切换保留未保存草稿，公共保存操作保留。同步 cms/index.html。

CMS 产教模型按“课程确认关联专业 → 按专业匹配产业链 → 确认产业链 → 开启模型”执行。新配置不预置专业；在产教模型页通过独立“选择教育部备案专业”弹窗（本科/职教、搜索、分页、单选、取消/确定）明确确认后展示产业链。基础信息仅展示关联结果，不提前匹配。取消不改动现有关联；改专业清空旧链和开关。同步 cms/index.html。

2026-09-08：岗位生成“课程关联”步骤暂不展示人培方案选择及相关引导，只保留课程专业；忽略历史草稿的人培筛选，保留岗位来源。同步 index.html、offline.html。

2026-09-08：岗位任务生成不展示前序步骤的历史问答及“我的选择”卡片；仅展示当前步骤的 Markdown 对话提示，提示呈现完成后再展示操作表单。保留“上一步”和岗位→单一典型工作任务→能力项链路，覆盖此前保留历史回答记录的要求。同步 index.html、offline.html。

2026-09-08：岗位与任务步骤在对话提示下方、表单上方突出展示一条推荐岗位，下方列表默认选中该岗位并标记“推荐”；只默认岗位，典型工作任务与能力项仍需确认。推荐限定当前专业匹配岗位，搜索及返回步骤不得覆盖手动选择。同步 index.html、offline.html。

2026-09-08：岗位选择、单一典型任务、能力项使用独立编号卡片和明显留白分隔；教学化转化各字段提供可通过校验的默认草稿，不用“请具体填写”充当默认值；Markdown 提示稍放慢。审阅步骤的项目说明、每阶段任务说明可原位编辑，保留返回时的修改与主动清空，最终创建使用审阅后的内容；修改上游输入则重新生成。同步 index.html、offline.html。

2026-09-08：项目式学习整体不展示或设置课时，包括阶段标题、编辑表单、教学化转化、审阅及任务说明；新生成项目不分配课时，旧草稿自动生成的建议用时不再展示。同步 index.html、offline.html。

2026-09-08：岗位项目生成在教学化转化与审阅之间增加“知识点匹配”，明确询问是否匹配；按任务确认当前图谱真实节点及 ID，不把岗位能力文本作为任务知识点内容。支持暂不匹配和空图谱，保留能力元数据与审阅编辑；Markdown 逐字展示适当延长。同步 index.html、offline.html。

2026-09-08：CMS 产教模型移除产业链状态标签（含“规则推荐 · 待人工确认”）和独立启用开关及前置提示。关联专业后，勾选至少一条有效产业链即开启，全部取消即关闭，保存后生效；历史开关值不覆盖产业链选择。覆盖此前单独开启模型的规则，同步 index.html、offline.html、cms/index.html。

2026-09-08：岗位与任务步骤选定典型工作任务后，知识、技能、素养能力项默认全部勾选；辅助生成能力项同样默认全选。允许手动取消，返回步骤或再次点击当前任务保留已调整的选择。覆盖此前能力项不默认选择的规则，同步 index.html、offline.html。

2026-09-08：岗位生成的知识点匹配为每个任务默认勾选最多 3 个可用知识点，名称匹配优先，不足时按课程图谱顺序补充；允许手动增减或清空，返回步骤保留调整，空图谱不虚构知识点。同步 index.html、offline.html。

2026-09-08：岗位与任务步骤的推荐岗位只用一行轻量文案，岗位名称加粗并使用蓝色；不使用强调卡片、底色、边框、图标或状态标签。同步 index.html、offline.html。

2026-09-08：岗位与任务步骤的“选择能力项”卡片标题右侧不展示“模拟数据 · 可编辑”等来源状态标签，保留底层来源信息。同步 index.html、offline.html。

2026-09-08：推荐岗位文案并入“岗位与任务”助手 Markdown 有序列表第 4 点，与前三点一同逐字展示，岗位名称加粗标蓝，不作为独立提示块。覆盖上一条独立一行提示的实现；同步 index.html、offline.html。

2026-09-08：项目说明岗位图谱与预览图谱须读取当前 CMS 已保存的专业—产业链选择，历史项目同步生效。课程专业关联与岗位自身产业归属保留各自来源；未确定岗位环节时不得据专业推断。同步所有静态入口。

2026-09-08：岗位项目生成弹窗采用左侧纵向五步导航、右侧独立滚动的对话与操作区域；步骤推进按钮统一为“确认”，最终覆盖按钮保留明确的覆盖文案。同步 index.html、offline.html。
