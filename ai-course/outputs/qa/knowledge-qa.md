# 知识图谱还原检查

final result: passed

## 参考与范围

视觉参考：用户最初提供的知识图谱截图 `codex-clipboard-7c17d17f-b2db-4004-9c50-def22bda4077.png`。复用专业建设 `src/App.vue` 课程模型的节点详情/编辑/思政设计交互组织，按新截图重做图谱画布。

数据来自 `/Users/liuhongzhe/Desktop/智能制造图谱生成/build-intelligent-manufacturing-graph.mjs` 的声明数据，未执行原工作簿生成或修改原文件。快照来源写入 `src/knowledge/seed.json`。重新计算得到 1/8/28/84 四级节点，共 121 个，其中 20 个思政点。资源关联为零，未伪造截图中的已关联资源。

## 视觉比较

- CSS 视口 1728 × 953；参考排除浏览器工具栏（原图 3600 × 2100，裁切 112,162 至 3600,2100）。
- 实现 `knowledge-final.png`，归一化全图并列 `knowledge-comparison.png`，图谱与图例局部并列 `knowledge-focus.png`。
- 内置浏览器 fullPage 导出的内容位于画布左上方，以 0.5 倍比例渲染。裁掉空白画布并将源图同尺度缩至 864 × 480；不将重采样模糊当作产品字体问题，不宣称像素级复制。
- 字体/层级：中文系统字体，统计总量、层级、节点名称、节点计数与说明分别分级。长二级名称按截图截行。
- 布局/间距：左侧浮动图例、圆形画布、右上两组视图工具与编辑、右下缩放，保留课程导航。
- 色彩：紫色根节点、蓝色二级节点、青绿色三级节点、浅蓝四级节点；白色圆盘、淡蓝刻度、细连接线。
- 资产：采用 ECharts SVG 图表渲染器生成可交互的数据图谱，Phosphor 功能图标；不使用截图铺底。
- 文案：知识点名称与原课程数据一致，节点详情保留知识说明、关系与思政设计。浏览器内第二个项目为之前测试留下的示例，不属于图谱数据。

## 修正记录

1. 首次渲染缺少 GraphicComponent 注册，背景环未显示，控制台报告组件缺失。已注册并重载，补上白色圆盘和环形刻度；修正后记录无新增 error。
2. 图谱专属底部入口在多项目侧栏中被挤出视口。已将「开放图谱到班级 / 管理资源」固定于侧栏底部并预留滚动空间。
3. 白色底盘初版仅有环形轮廓；比较后补足内部白色填充，最终截图确认与参考的明暗关系接近。

## 验证

- 浏览器：搜索四级知识点、点击图上中心节点、打开详情、查看先后修/思政设计、空名称校验、保存、目录、20 个思政点筛选、110% 缩放、重置、全部节点与关系切换。
- 1200 × 900 紧凑桌面：scrollWidth = innerWidth = 1200，无页面水平溢出。
- 业务和离线测试 10/10；打包测试 4/4；生产构建通过。新增 file-origin JSDOM 测试实际执行完整离线 HTML，确认 SVG 图谱挂载、搜索及详情打开。
- index.html 与 offline.html 同源重建，继续保留脚本在页面容器之后执行的修复。浏览器 file:// 视觉验证仍受工具 URL 策略限制，未绕过。
- 单文件 JS 约 776 KB，构建提示超过常规 500 KB 阈值；为保持双击打开时无模块加载依赖，本次有意维持单包。

## 边界与后续微调

- P3：部分图标、光晕和节点角度与原系统有轻微差异；品牌素材仍为独立原型的图标。
- 开放图谱到班级仍说明未连接教学班级；学习资源和习题未接入。
- 手机端与大规模图谱性能不属于本次验收范围。
- 无待处理 P0/P1/P2。
